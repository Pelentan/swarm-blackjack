# Bank Service Configuration
# Copy to bank.env and fill in values
# bank.env is gitignored — never commit real credentials

# Bank database — only bank-service has these credentials
# In production: inject via Docker secrets or vault
BANK_DB_HOST=bank-db
BANK_DB_PORT=5432
BANK_DB_NAME=bankdb
BANK_DB_USER=bankuser
BANK_DB_PASSWORD=change-me-in-production
