{
  "name": "document-service",
  "version": "0.1.0",
  "description": "PDF document generation service — layout engine, no domain knowledge",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "pdfkit": "^0.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pdfkit": "^0.13.0",
    "typescript": "^5.0.0"
  }
}
